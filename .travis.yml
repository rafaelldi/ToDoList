services:
  - docker
    
branches:
  only:
    - master

script:
  - docker build -t todolist .
    
deploy:
  - provider: script
    script: sh docker_push
    on:
      branch: master
      
after_deploy:
  - sh heroku_release